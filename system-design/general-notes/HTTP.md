# RPCs
- Client Interface + Client Stub -> RPC runtime <- Server Stub + Server Interface
- Client & Server run on different processes
- Appears as just a function call locally, but when called it is communicating over a socket with the server instance

